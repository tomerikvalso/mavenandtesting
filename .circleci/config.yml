version: 2.1
orbs:
  maven: circleci/maven@2.1.1
jobs:
  jobpremaven:
    docker:
      - image: cimg/base:2026.07
    type: build # or release, approval, no-op
    steps:
      - checkout
      - setup_remote_docker:
          docker_layer_caching: true
      - run:
          name: Save test results
          command: |
            mkdir -p ~/test-results/junit/
            find . -type f -regex ".*/target/surefire-reports/.*xml" -exec cp {} ~/test-results/junit/ \;
          when: always
      - store_test_results:
          path: ~/test-results

  jobincludemaven:
    machine: true
    type: build
    steps:
      - checkout

      - run:
          name: Build an image
          command: |
            docker build --tag japp .
      - run:
          name: Run application in background
          command: |
            docker run -it --rm japp \
            curl -X GET http://localhost:8080/HEI/

          background: true

      - run:
          name: Create CHcommand2
          command: chmod 755 zap/wrk/zap.yaml

      - run:
          name: "lag hei"
          command: mkdir /home/circleci/project/target/hei

      - run:
          name: ZAP baseline scan of application
          command: |
            (         
            docker pull ghcr.io/zaproxy/zaproxy:bare && \
            docker pull zaproxy/zap-bare && \
            docker run -u root -v $(pwd):/zap/wrk:rw --network="host"  zaproxy/zap-stable zap-baseline.py -t http://localhost:8080 -r scan-report.html
            
            )

  jobdeploy:
    docker:
      - image: google/cloud-sdk
    working_directory: ~/dev2

    steps:
      - run:
          name: "Say hello"
          command: "echo Hello, World!"
      - run:
          name: Create app.yaml
          command: chmod +x app.yaml.sh && ./app.yaml.sh > ./app.yaml
      - run:
          name: Set up GCloud
          command: |
            echo ${GOOGLE_PROJECT_ID} | 
            echo $GCLOUD_SERVICE_KEY | base64 --decode | gcloud auth activate-service-account --key-file=-
              gcloud --quiet config set project ${GOOGLE_PROJECT_ID}

workflows:
  workflowpremaven: 
    jobs:
      - jobpremaven:
          name: jobpremaven
          post-steps:
            - run:
                name: "Say hello"
                command: "echo Hello, World pre!"
          filters:
            branches:
              only: circleci-project-setup
  maven_test:
    jobs:
      - maven/test:
          command: '-X verify'
  workflowpostmaven:
    jobs:
      - jobincludemaven:
          name: jobincludemaven
          post-steps:
            - run:
                name: "lag hei"
                command: mkdir /home/circleci/project/target/hei
          filters:
            branches:
              only: circleci-project-setup
